# AGENTS.md — 项目协作总纲（AI 开发 agent 必读）

> 读这个文件，你就知道该怎么和项目负责人协作、从哪里开始、做到哪一步算"完成"。

## 1. 项目是什么（30 秒版）

《昨夜有雨》RainingYesterday：一款基于现实地理空间的生活模拟 RPG。玩家用真实定位解锁地图迷雾，把经过的地点、天气、人物、事件记录成"我的世界"，探索、养成、种田、建造、NPC 交互、实拍识别，并在生活之下触达传闻、异常与历史地图。

- 平台：Android（Kotlin + Jetpack Compose）
- 形态：单模块应用 + 数据驱动内容引擎
- 当前阶段：MVP = 里程碑 M0 → M9（见 `docs/05_ROADMAP.md`）

## 2. 先读什么（阅读顺序）

| 顺序 | 文件 | 什么时候读 |
| --- | --- | --- |
| 1 | `GDD_v2.md`（根目录） | 理解产品与玩法全貌 |
| 2 | `docs/00_INDEX.md` | 入口与文档地图 |
| 3 | `docs/01_ARCHITECTURE.md` | 分层、包结构、核心引擎（动代码前必读） |
| 4 | `docs/02_DATA_MODEL.md` | 内容 JSON schema（写数据层/内容/编辑器前必读） |
| 5 | `docs/03_TECH_STACK.md` | 技术选型、依赖、权限 |
| 6 | `docs/04_CONVENTIONS.md` | 代码规范（每次写代码遵守） |
| 7 | `docs/05_ROADMAP.md` | 里程碑与验收标准（开始每阶段前读） |
| 8 | `docs/06_ART_AND_AUDIO.md` | 美术与音乐风格（做 UI/视觉/AR 贴片/音效前读） |

## 3. 三条铁律（不可违反，违反 = 返工）

1. **内容数据驱动**：地点、NPC、任务、配方、事件、传闻、异常、微仪式、生灵，全部是 JSON 数据，代码里不硬编码内容。加内容 = 改 JSON，不改引擎。
2. **领域层纯 Kotlin**：`domain/` 不得 import Android、地图、Compose、CameraX、ARCore、传感器。规则可独立单测。
3. **单向数据流**：UI 只读状态、传 intent；所有状态变更走 `ViewModel → 领域引擎 → StateFlow`。

## 4. 怎么工作（协作方式）

- **动代码前先一句话说你要做什么；做完一句话说改了什么**。保持透明，方便负责人把关。
- **一次只做一个里程碑**（或一个验收单元）。接到任务先读 `05_ROADMAP.md` 里该里程碑"涉及文档"指到的章节，再动手。
- **不越界**：只做本里程碑范围。不顺手重构、优化、重命名范围外的文件；发现范围外的问题记录下来报告，不要擅自动。
- **遇歧义停下来问**：字段含义不确定查 `02`；仍不确定就问负责人，不要猜。
- **遵循既有模式**：不引入文档之外的架构模式（如不必要的抽象层、拆分多 module）。
- **小步提交**：按文件/模块小步 commit，别"一大坨"。

## 5. 完成定义（Definition of Done）

一个里程碑"完成"意味着以下**全部**满足：

1. 能编译（`gradlew assembleDebug` 或等效命令通过）。
2. 领域层单测通过（如该里程碑涉及领域逻辑）。
3. 该里程碑"验收"列的所有条目通过。
4. 无内容硬编码、无领域层越界、无文档外新模式（对照 `04` 反模式清单自查）。
5. 改动范围只在这个里程碑内，范围外文件没有改痕。

## 6. 提交规范

- 提交信息：祈使句 + 范围前缀，例 `feat(engine): 触发引擎支持 time_range 条件`。
- 除非负责人明确要求，不自行 commit / push 到远端。
- 不提交：密钥、`google-services.json`、玩家存档、生成产物、`build/`。

## 7. 反模式速查（不要做）

- 把地点名 / NPC 台词 / 配方 / 内容写进代码
- 把世界内容写进 Room
- 领域层 import Android / Compose / 地图 / CameraX / ARCore / 传感器
- UI 直接改 `GameState`
- 引入通用图像识别模型
- 为"将来可能"引入抽象层
- 顺手重构范围外的代码

## 8. 当前开始

从 `docs/05_ROADMAP.md` 的 **M0 · 工程脚手架** 开始。每完成一个里程碑，若新增了文档就更新 `docs/00_INDEX.md` 的文档地图，并在汇报里说明：**改了什么、为什么、如何验证**。